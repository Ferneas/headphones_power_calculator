<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color -->
        <item name="windowSplashScreenBackground">@android:color/background_dark</item>
        <!-- Set the theme of the activity that follows the splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.HeadphonesPowerCalculator</item>
    </style>
</resources>